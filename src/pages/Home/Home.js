import { Banner } from '../../components'
import { CardContainer } from '../../containers'

export const Home = () => {
  return (
    <section>
        <Banner />
        <CardContainer />
    </section>
  )
}

export default Home